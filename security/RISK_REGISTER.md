# ADMA Digital — Risk Register

**Effective:** 2026-08-05
**Review cadence:** quarterly at minimum, and after any incident (per `INCIDENT_RESPONSE_PLAN.md`) or major feature push. The point of this document isn't sophistication — it's evidence of a repeating process (CAIQ Phase 3 item 17).

**Scoring:** Likelihood and Impact each rated Low/Medium/High. Risk = the combination, not a formula — used to order the backlog, not to pretend false precision.

| # | Risk | Likelihood | Impact | Risk | Owner | Status | Mitigation | Next review |
|---|---|---|---|---|---|---|---|---|
| 1 | No independent penetration test has ever been performed | Medium | High | **High** | Platform operator | Open | Scope + cost estimate ready in `PENTEST_SCOPE.md` (~$1,500–4,000 for a regional-firm scoped engagement); needs budget approval to commission (CAIQ Phase 3 item 15) | Before next major feature push, or 2027 site plan launch |
| 2 | Single EC2 instance, no redundant/multi-AZ infrastructure | Low (no incident to date) | Low | ~~Low~~ **Closed** | Platform operator | **Option 3 (ALB) fully cut over 2026-08-31.** | `admadigital.co.zw`/`www` now resolve (Route53 ALIAS) to `adma-alb`, which load-balances both EC2 instances (both `healthy`), terminates TLS with a real ACM cert, redirects HTTP→HTTPS at the listener, and sits behind WAF (AWS Managed Rule Groups). Security group tightened to ALB-only on 80/443, confirmed live traffic still flows correctly after. One brief self-caused outage during this rollout: the security group was tightened once *before* DNS actually pointed at the ALB, blocking all direct traffic since nothing was routed through the ALB yet — caught via a certificate-mismatch report, reverted within minutes, then redone in the correct order (listener → DNS → verify → *then* re-tighten SG) | Rehearse promotion runbook (still relevant for a primary-instance failure even with the ALB in place) |
| 3 | `server/.env` secrets and nginx config exist only on the EC2 instance, no off-instance backup | Low | High (re-provisioning every credential from scratch on instance loss) | **Medium** | Platform operator | **Nginx half mitigated 2026-08-28** | The nginx side is now backed up — `security/nginx/adma.conf` and `gzip.conf` are point-in-time snapshots in the repo (not auto-synced; re-capture after manual nginx changes). `.env` secrets are still instance-only, no backup — that's the remaining open part, tracked in `DISASTER_RECOVERY_PLAN.md` §5 | Next DR plan review; consider a secrets manager (not git) for the `.env` half |
| 4 | AWS CloudTrail not enabled — no audit trail of AWS-account-level actions | Medium | Medium | ~~Medium~~ **Closed** | Platform operator | **Closed 2026-08-05.** Verified genuinely delivering log files, not just configured — see `CLOUDTRAIL_SETUP.md` | — | — |
| 5 | No centralized log shipping (CloudWatch) — security logs live only in pm2's local stdout capture | Medium | Medium | ~~Medium~~ **Closed** | Platform operator | **Closed 2026-08-05.** CloudWatch agent shipping pm2 logs, verified receiving real events, 365-day retention — see `CLOUDWATCH_LOGGING_SETUP.md` | — | — |
| 6 | CSP still in Report-Only mode — not actually blocking any injection in production | Medium | Medium | **Medium** | Platform operator / Claude | Open, in progress | 3 real gaps found and fixed 2026-08-05 (`i.ytimg.com`, `blob:`, Zoom); needs one more clean observation window before flipping to enforcing (learned from the 2026-07-26 incident — never flip on an assumption again) | Re-check violation logs in ~2-4 weeks, then flip if clean |
| 7 | Two frontend dependencies have unpatched high/critical CVEs (`react-router`, `pdfjs-dist`) needing major version bumps | ~~Low (pdfjs-dist has no live attack surface)~~ **Medium as of 2026-08-28 — see below** / Low-Medium (react-router is web-reachable but CVE is narrow — RSC mode CSRF, a mode this app doesn't use) | Medium (CVSS 8.8 — arbitrary JS execution on opening a malicious PDF) | ~~Low-Medium~~ **Medium** | Platform operator / Claude | Open, tracked, likelihood increased | CI (`security-audit.yml`) surfaces this on every push; fix requires dedicated regression testing of the app's routing before the `react-router` major bump. **2026-08-28 investigation (pdfjs-dist half):** confirmed all 4 `npm audit` findings (1 critical `tar`, 3 high) are one chain — `pdfjs-dist` (was devDependency-only at investigation time) optionally pulls `canvas` → `@mapbox/node-pre-gyp` → `tar`. Attempted the `pdfjs-dist` 3.11.174→5.6.205 bump: fixed the `legacy/build/pdf.js`→`pdf.mjs` import-path break and the `workerSrc` API change, but hit a **native segfault** in `@napi-rs/canvas` during actual page rendering — v5 needs a matching `@napi-rs/canvas` bump too, not just an import fix. Reverted; `pdfjs-dist` left at 3.11.174. **Same day, separately: `pdfjs-dist` moved from devDependency to a real client-side dependency** — a new PDF-logo-upload feature (`renderPdfFirstPageToBlob` in `src/lib/imageUtils.js`) now runs it in the browser against exhibitor-supplied PDF files, on both the exhibitor portal and the public signup form. This directly contradicts the "no live attack surface" framing above — the known CVE (arbitrary JS execution on a malicious PDF) is now live, reachable by anyone who can upload a "logo," and would execute in that uploader's own browser session. Needs its own dedicated regression pass (same as `react-router`), off production infra | Elevated priority given live exposure — next dependency-focused pass; upgrade `pdfjs-dist` + `@napi-rs/canvas` together, validated against a real magazine PDF outside production |
| 8 | No customer-managed KMS encryption keys — relies entirely on AWS default encryption | Low | Low (no customer has asked; AWS default encryption is genuinely adequate at this scale) | **Low** | Platform operator | Accepted for now | Documented in `DATA_CLASSIFICATION_AND_RETENTION.md` §4 | Revisit only if a partner/customer specifically requires it |
| 9 | No independent legal review of the Privacy Policy / no formal Data Processing Agreements with vendors | Low | Medium (regulatory/contractual exposure, not a security exploit) | **Low-Medium** | Platform operator (needs outside legal help) | Open | Self-review pass done, flagged as non-legal-advice in `PRIVACY_POLICY_REVIEW_NOTES.md` | Before any enterprise/large-partner contract that would ask for a DPA |
| 10 | No formal onboarding/offboarding or background-screening process | Low (team is currently 1-2 people, all long-known) | Medium (grows fast once hiring starts) | **Low** | Platform operator | Prepared, not yet needed | Checklist ready in `ONBOARDING_OFFBOARDING.md` for whenever hiring starts | Activate at first hire |
| 11 | S3 bucket versioning/backup status not yet reviewed | Unknown (not yet assessed) | Medium (uploaded exhibitor images/video, EFT proof-of-payment documents) | **Medium (unknown — treat as open until assessed)** | Platform operator / Claude | Open | Flagged in `BACKUP_AND_RESTORE.md` §5 as not yet reviewed | Next backup/DR-focused pass |
| 12 | Payment dispute/fraud handling is informal (no documented chargeback/dispute procedure beyond the general Incident Response plan) | Low (no incidents to date) | Medium (real money, real customers) | **Low-Medium** | Platform operator | Partially covered | `INCIDENT_RESPONSE_PLAN.md` covers "payment fraud" generically; a Paynow-specific dispute procedure isn't separately documented | Next payments-focused pass |
| 13 | Both EC2 instances have only 912Mi RAM and zero swap — a routine maintenance task (dependency reinstall, bulk copy, native compile) run directly on-instance can exhaust memory and take the site down | Medium (proven — caused a real SEV1 on 2026-08-28, see `INCIDENT_RESPONSE_PLAN.md` incident log) | High (no load balancer; primary serves live traffic directly, so instance unresponsiveness = full outage) | **High** | Platform operator | Open | Immediate mitigation applied: heavy operations now use a disk-backed path and targeted/`--omit=optional` installs instead of full reinstalls directly on these instances. Underlying fix (add swap, or bump instance size) not yet done | Before the next dependency-major-version pass; ideally before any other memory-heavy maintenance |
| 14 | gzip compression was never actually enabled in nginx — every static asset (JS/CSS) shipped full-size instead of compressed | Medium (a plain misconfiguration, not exploit-driven, but real user impact) | Low (performance/UX, not a security exploit — the main JS bundle was taking 70+ seconds to load) | ~~Low~~ **Closed** | Platform operator / Claude | **Closed 2026-08-28.** `gzip on;` had only ever existed commented-out in the unused `nginx.conf.default` template. Added `security/nginx/gzip.conf`, applied to both instances, verified: JS bundle load time 70.9s → 718ms | — | — |
| 15 | `adma-vscode-cli` (the interactive/CLI IAM user used for local admin work) carried `AmazonEC2FullAccess` and `AmazonS3FullAccess`, both far broader than anything actually used — and this account holds S3 buckets belonging to unrelated projects (`blessing-roora`, `minecon`, `minecon-knowledge-base-2026`) this key could delete from | Low (single-operator account, not multi-tenant in the adversarial sense) | Medium (blast radius extended to unrelated projects' data, not just this app) | ~~Low-Medium~~ **Closed** | Platform operator | **Closed 2026-08-28.** Replaced with `ADMA-EC2-ReadOnly` (Describe-only) and `ADMA-S3-AdmaZwScoped` (writes limited to `adma-zw`; `ListAllMyBuckets` kept, name-listing only). Verified: EC2 `CreateTags` and S3 access to `blessing-roora` both now cleanly denied; `adma-zw` access and EC2 describe both still work | — | — |
| 16 | New video-compression Lambda (`adma-video-compress`, added 2026-08-28) has no CloudWatch alarm on invocation errors/throttles, and was built by hand via the AWS CLI rather than IaC | Low (low-volume feature; a failure surfaces to the uploader as a timeout, just not to the platform operator) | Low | **Low** | Platform operator / Claude | Open | See `security/VIDEO_COMPRESSION_LAMBDA.md` "Known gaps" section for the full list | Next infra/observability-focused pass |
| 17 | Both EC2 instances' shared security group (`sg-0399ccd0797f2d0bd`) allowed inbound 80/443 from `0.0.0.0/0` — direct-IP access bypassing any future ALB/WAF entirely | Medium (trivially discoverable — anyone who resolves or scans the instance's IP) | Medium (bypasses WAF, and would let a spoofed `X-Forwarded-Proto` header defeat the redirect-loop fix below) | ~~Medium~~ **Closed** | Platform operator / Claude | **Closed 2026-08-31.** Restricted to `sg-09d2b7744e18e0aa3` (the ALB's own security group) on both 80 and 443; verified the ALB still reaches both instances (`healthy`) immediately after. `adma.tyflex.co.zw` direct access is retired as a result — confirmed legacy/unused | — | — |
| 18 | nginx's port-80 redirect for `admadigital.co.zw`/`www` was unconditional — once the ALB started forwarding decrypted (plain-HTTP) traffic there, it would have looped forever (ALB→nginx→301 https→ALB→nginx→301 https→...) | High if the ALB cutover had gone ahead without this (would have taken the site down the moment DNS switched) | High | ~~High~~ **Closed** | Platform operator / Claude | **Closed 2026-08-31.** Redirect now conditional on `X-Forwarded-Proto` (nginx `if` can't combine conditions natively, hence a two-step `set`/`if`); only safe because #17 above closed first (otherwise the header could be spoofed by hitting the instance directly). Verified with direct curl tests simulating both an ALB-forwarded originally-HTTPS request (200, served normally) and a genuine direct plain-HTTP request (301, still redirects) | — | — |
| 19 | The `admadigital.co.zw`/`www` A records carried a second, stray IP (`102.222.22.154`) alongside the real instance IP — copied faithfully from DirectAdmin into Route53 during the earlier DNS migration without questioning why there were two, and never resolved. It served the TLS certificate for a completely unrelated server (`ns1.named.co.za`), so browsers hitting that IP by chance saw a hard "connection not private" warning | Medium while it existed — silent until NS fully cut to Route53, at which point it became live and immediately visible (browsers alternate between multi-value A records) | High (looks exactly like a MITM warning to a real visitor — direct credibility/trust damage) | ~~High~~ **Closed** | Platform operator / Claude | **Closed 2026-08-31**, reported by the platform operator directly (a live cert warning). Removed the stray IP from both records, TTL dropped to 300s for fast propagation, verified the correct cert (`CN=admadigital.co.zw`) is now served consistently | — | — |
| 20 | `VideoUploadOrUrlField.jsx` (booth video, ad-carousel videos, magazine video ads — 6 call sites) deleted the *previous* video from S3 the moment any new upload finished inside it, based on the field's local draft value — not on whether that replacement was ever actually saved. A second upload before the first was ever saved (or even a single upload followed by Cancel) deleted the still-live, still-DB-referenced video out from under the exhibitor, leaving their booth page pointing at a 404 while the actual replacement sat orphaned, unreferenced, in S3 | Medium (only triggered by re-uploading/retrying — but retrying is exactly what an exhibitor does when a video "isn't playing," which compounds the damage instead of fixing it) | Medium (exhibitor-visible broken video, not a security exploit — no data exfiltration, but real customer-facing damage and a support burden) | ~~Medium~~ **Closed (partial)** | Platform operator / Claude | **Closed 2026-08-31** for the deletion bug itself — reported live for exhibitor `e13` (Haingate), reconstructed from the S3 object timeline (3 keys: 2 orphaned survivors + 1 deleted key still referenced by the DB), confirmed root cause, removed the premature auto-delete entirely. Exhibitor's `video_url` restored to the verified, still-existing, correctly-compressed replacement. **Not yet done:** a proper "delete only after a confirmed save" cleanup per call site — for now, replacing a video just orphans the old S3 object (storage cost, not data loss) rather than cleaning it up | Next storage-cost-focused pass; retrofit cleanup into each of the 6 call sites' actual save mutations |
| 21 | Two exhibitor videos (Terramera `e20`, Purleigh `e131`) were uploaded before the video-compression Lambda existed (pipeline deployed 2026-08-28; these landed 2026-08-13 and 2026-08-25) and were never backfilled — one was 213MB (bypassed the app's own 200MB browser cap entirely, uploaded directly to S3 by staff), the other 40.6MB, both served raw and unprocessed to visitors | Low (one-time legacy gap, not a recurring code defect — the pipeline itself works correctly for everything uploaded after 2026-08-28) | Medium (large raw video failing to load/play is exhibitor-visible, same user impact as risk #20 despite being a different root cause) | ~~Medium~~ **Closed** | Platform operator / Claude | **Closed 2026-08-31.** Found by scanning the whole `video-ads/` prefix for objects over the 20MB compression target lacking `processed:true` metadata, after investigating the Terramera report. Both manually re-invoked through `adma-video-compress` (same Lambda, no code change needed) — Terramera 213MB→16.2MB, Purleigh 40.6MB→21MB, both verified publicly reachable with valid MP4 headers. Bucket-wide re-scan confirmed no other oversized/unprocessed videos remain | — | — |
| 22 | The pre-login MFA/OTP/forced-password-change/password-reset challenge store (`server/routes/auth.js`) was a plain in-memory `Map` — private to one Node process. Once the ALB Phase B cutover (risk #2) put two separate EC2 instances behind the load balancer with no session affinity, a login flow's first request (submit password, get a challenge token) and its second (submit the OTP/code moments later) had no guarantee of landing on the same instance — the second instance had never heard of the token, surfacing as "Session expired. Please log in again." to users doing everything correctly and promptly. Real post-login sessions (`adma_auth_sessions`, `session.js`) were already correctly DynamoDB-backed for exactly this reason; this was the one piece of auth state that never got the same treatment | Medium (only affects the ~10-30 second window of an in-progress login, and only when the ALB happens to route the two requests to different instances — but with 2 targets round-robining, that's roughly a coin flip on every affected login) | Medium (login-blocking, platform-wide, but not a data exposure — a failed login attempt, not a successful bypass) | ~~Medium~~ **Closed** | Platform operator / Claude | **Closed 2026-08-31.** Reported live ("login process challenges... Session expired"). New DynamoDB table `adma_auth_challenges` (TTL on `expires_at_ttl`, same pattern as `adma_auth_sessions`), new `server/lib/challengeStore.js` module, all ~20 call sites in `auth.js` (email/SMS OTP, TOTP + first-time setup, forced password change, password reset, team-member invite/resend) refactored from the Map to it. A genuine, previously-unforeseen consequence of the ALB multi-instance cutover — not caught by that work's own verification, which tested infra reachability/health, not this in-flight-request-affinity edge case in application logic | — | — |
| 23 | `OMNIFLEX_API_KEY` (ADMA's own direct OmniFlex credential, `lib/omniflex.js`) appears unable to use the generic `/api/sms/send` endpoint — every call through `sendSms()` fails with `"This key does not belong to this workspace."` The OTP-campaign send path (`sendSmsOtp`, a different OmniFlex endpoint, same key) works fine and always has — this is why it went unnoticed: `sendSms()` was previously only called from `smsSilent()` (meeting confirmations etc.), which catches and logs failures without surfacing them to any user, so nobody would have seen this until a feature made the failure count user-visible | Medium (silent until something surfaces it — likely been failing since before this session, undetected) | Medium (every SMS notification that goes through the generic send path — meeting confirmations, and now broadcasts — has been silently not sending) | **Open** | Platform operator (needs a correct/re-scoped OmniFlex API key) | Found 2026-09-01 investigating a live report ("tried sending SMS they all failed") — confirmed via raw pm2 logs (identical `OMNIFLEX_API_KEY` hash on both instances, ruling out config drift; the error is a genuine workspace/permission mismatch on OmniFlex's side, not an ADMA code bug). Needs a valid API key for the workspace `/api/sms/send` actually targets, from OmniFlex's own dashboard — not something fixable from this codebase alone | Get a corrected/re-scoped key from OmniFlex; once done, spot-check `smsSilent()`'s call sites (meeting confirmations) have actually been silently failing this whole time as suspected |
| 24 | The new broadcast feature (risk #23's sibling, same day) sent up to 10 emails concurrently through the single shared `NoReply@tyflex.co.zw` Graph API mailbox — Microsoft Graph enforces a low per-mailbox concurrent-request limit, so a third of a real 21-recipient email broadcast failed with `"Application is over its MailboxConcurrency limit"` (a 429) | Medium (proven — happened on the first real broadcast) | Low (a partial-send undercount, recoverable by resending; not data loss or exposure) | ~~Medium~~ **Closed** | Platform operator / Claude | **Closed 2026-09-01.** Found investigating the same "SMS all failed" report (a separate bug from #23, surfaced by the same test). Email concurrency dropped to 3 (SMS keeps 10 — no evidence OmniFlex has the same issue), plus one retry with a short randomized delay specifically for a 429/concurrency error before counting a send as failed | Watch the next real multi-recipient email broadcast for any remaining 429s before assuming 3 is low enough |

## Risks resolved since the original 2026-08-04 assessment

For context — these rows existed in earlier thinking about this platform's risk posture and are now closed, kept here as a record rather than silently dropped:

- ~~No DynamoDB backup / PITR~~ — **Closed 2026-08-05.** Enabled fleet-wide, restore tested end-to-end. See `BACKUP_AND_RESTORE.md`.
- ~~No Incident Response plan~~ — **Closed 2026-08-05.** See `INCIDENT_RESPONSE_PLAN.md`.
- ~~Single superadmin account, no segregation of duties~~ — **Closed 2026-08-05** (turned out a second account already existed; a real authz bug that prevented it from working was fixed).
- ~~MFA optional/bypassable for console roles~~ — **Closed 2026-08-05.** See the CAIQ plan doc, Phase 2 item 8.
- ~~No application-level or nginx-level rate limiting~~ — **Closed.** App-level existed already; nginx-level added and load-tested 2026-08-05.
- ~~AWS CloudTrail not enabled~~ — **Closed 2026-08-05,** via CloudShell. Verified real log files delivering to S3.
- ~~No centralized log shipping~~ — **Closed 2026-08-05.** CloudWatch agent live, verified receiving events.
- ~~Single EC2 instance with no automated backup or recovery~~ — **Mitigated 2026-08-05.** Daily DLM snapshots + auto-recovery alarm live.
- ~~No redundant compute — one instance is the whole platform~~ — **Mitigated 2026-08-06.** Warm standby live in a different AZ, verified reachable/healthy end-to-end. See `PROMOTION_RUNBOOK.md`.

---
*Part of ADMA Digital's CAIQ v4.0.3 remediation plan — see `ADMA_CAIQ_Assessment_and_Security_Plan_2026-08-04.md`, Phase 3 item 17.*
